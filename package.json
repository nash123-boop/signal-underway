{
  "name": "signal-underway",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "node ./scripts/lint-frontmatter.mjs",
    "check:links": "node ./scripts/check-links.mjs",
    "check:design": "node ./scripts/check-design.mjs",
    "check": "npm run lint && npm run check:links && npm run check:design && astro build",
    "new:post": "node ./scripts/new-post.mjs"
  },
  "dependencies": {
    "astro": "^4.16.0"
  }
}
